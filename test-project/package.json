{
	"devDependencies": {
		"@types/jest": "^24.0.23",
		"jest": "^24.9.0",
		"rimraf": "^3.0.0",
		"ts-jest": "^24.1.0",
		"typescript": "^3.7.2"
	},
	"scripts": {
		"compile": "rimraf build && tsc -p .",
		"test": "jest"
	}
}
